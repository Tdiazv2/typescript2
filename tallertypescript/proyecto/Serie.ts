export class Serie {
    constructor(public id: number, public nom: string, public canal: string, public temporadas: number, public descripcion: string, public donde: string, public foto: string) {}
}
